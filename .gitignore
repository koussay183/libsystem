# dependencies
node_modules
.pnp
.pnp.js

# build
dist
dist-ssr
*.local

# env (contains your Firebase config — keep private)
.env
.env.local
.env.*.local

# firebase
.firebase
.firebaserc
firebase-debug.log
firestore-debug.log

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local Netlify folder
.netlify

# live client data pulled for safekeeping — never commit
backups/
cli/service-account*.json

# frozen config of the pre-SaaS project, for the one-time pull
.env.legacy
